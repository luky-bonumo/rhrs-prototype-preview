# rhrs — prototype preview

Public preview of the rhrs design prototype, served through GitHub Pages so it
can be opened in a browser and shared.

**Generated — do not edit by hand.** `index.html` is built from the private
`rhrs-prototype` repository by `tools/build-preview.py`. Any edit here is lost
on the next build; change the prototype instead.
